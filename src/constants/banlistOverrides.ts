import { BanOverrideRule } from '../types/ygo';

/**
 * No hand-written banlist overrides are currently approved. An override may
 * only be added with a traceable source and an automated passcode check.
 */
export const LATEST_BANLIST_OVERPRIDES: BanOverrideRule[] = [];
